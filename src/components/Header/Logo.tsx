import SAS from "../../assets/logo.svg";

const Logo: React.FC = () => {
  return (
    <img
      src={SAS}
      alt="Logo"
      className="bg-white px-4 py-2 rounded-lg mx-auto w-24 mt-4"
    />
  );
};

export default Logo;
