import SAS from "../../assets/logo.svg";

const Logo: React.FC = () => {
  return (
    <img
      src={SAS}
      alt="Logo"
      className="bg-white p-3 rounded-lg mx-auto w-24 mt-4"
    />
  );
};

export default Logo;
